# Sensei
Where lecture slides finally make sense

npm install next@latest

~$ python -m venv venv
~$ venv\\Scripts\\Activate.ps1
(venv) ~$ pip install -r requirements.txt
(venv) ~$ cd agent_backend
(venv) ~$ python start_server.py
